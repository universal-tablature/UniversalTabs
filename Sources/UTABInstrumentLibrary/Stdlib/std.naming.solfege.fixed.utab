module std.naming.solfege.fixed

naming FixedSolfege {
    register absoluteOctave
    note do = letter(C, 0)
    note re = letter(D, 0)
    note mi = letter(E, 0)
    note fa = letter(F, 0)
    note sol = letter(G, 0)
    note la = letter(A, 0)
    note ti = letter(B, 0)
}
